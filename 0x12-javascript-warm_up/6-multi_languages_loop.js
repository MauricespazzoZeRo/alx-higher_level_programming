#!/usr/bin/env node
// Script that uses loops to print text

let cMsg = 'C is fun';
let PyMsg = 'Python is cool';
let JvMsg = 'JavaScript is amazing';

for (let i = 0; i < 1; i++)
{
	console.log(cMsg + '\n' + PyMsg + '\n' + JvMsg);
}
